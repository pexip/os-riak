diff --git a/rel/files/app.config b/rel/files/app.config
index a646e78..e112e56 100644
--- a/rel/files/app.config
+++ b/rel/files/app.config
@@ -1,5 +1,6 @@
 %% -*- mode: erlang;erlang-indent-level: 4;indent-tabs-mode: nil -*-
 %% ex: ft=erlang ts=4 sw=4 et
+%% Default pexip riak app.config - will be replaced  after initial boot by a puppet-provisioned one
 [
  %% Riak Client APIs config
  {riak_api, [
@@ -9,12 +10,17 @@
             %% connections being initialised *simultaneously*, set this number
             %% higher.
             %% {pb_backlog, 64},
-             
-            %% pb is a list of IP addresses and TCP ports that the Riak 
+            %% pb is a list of IP addresses and TCP ports that the Riak
             %% Protocol Buffers interface will bind.
             {pb, [ {"{{pb_ip}}", {{pb_port}} } ]}
             ]},
 
+ %% Default bucket properties - see http://docs.basho.com/riak/latest/ops/advanced/configs/configuration-files/
+ {default_bucket_props, [
+            {allow_mult,false},
+            {last_write_wins,true}
+            ]},
+
  %% Riak Core config
  {riak_core, [
               %% Default location of ringstate
@@ -80,7 +86,11 @@
  {riak_kv, [
             %% Storage_backend specifies the Erlang module defining the storage
             %% mechanism that will be used on this node.
-            {storage_backend, riak_kv_bitcask_backend},
+            %% {storage_backend, riak_kv_bitcask_backend},
+            {storage_backend, riak_kv_memory_backend},
+            {memory_backend, [
+                        {ttl, 432000} %% 5 Days in seconds
+            ]},
 
             %% raw_name is the first part of all URLS used by the Riak raw HTTP
             %% interface.  See riak_web.erl and raw_http_resource.erl for
